# URL REST API бэкенда FastAPI без слэша в конце.
# Для npm run dev можно оставить пустым — Vite проксирует /health, /projects, /tasks на :8000.
# Для production-сборки укажите полный URL, например http://127.0.0.1:8000
VITE_API_BASE_URL=
